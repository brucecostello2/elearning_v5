"""
WP-41 demonstrations. Scripts, not library code — nothing imports this package.

  ``shadow_env.sh``               shared settings; sourced, not run
  ``demo_shadow_run.sh``          Task 2: the full run, start to final
  ``demo_resume.sh``              Task 3: SIGKILL mid-fan-out, resume
  ``demo_duplicate_delivery.py``  Task 3: one activity delivered twice,
                                  deterministically, with no cluster
"""
