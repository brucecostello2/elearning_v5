"""Shared FastAPI wrapper skeleton for IVGS v5 media servers (Build Plan section 6).

Import from the submodules directly:
    from common.base import create_app, get_model, run, vram_stats
    from common.jobs import JobStore, register_job_routes, Job, JobStatus
"""
