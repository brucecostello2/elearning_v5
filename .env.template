# ============================================================
# IVGS v5 Environment Variables — Appendix A.2 compliant
# Copy to .env and fill in secrets before deployment
# ============================================================
#
# SECURITY NOTICE:
# - All values marked CHANGE_ME_* MUST be replaced before deployment
# - Generate JWT_SECRET_KEY with: python3 -c "import secrets; print(secrets.token_hex(64))"
# - Generate DATABASE password with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# - NEVER commit .env files to version control (they are .gitignored)
# - See docs/deployment/runbook.md for full security checklist
#

# === Database (PostgreSQL 17) ===
DATABASE_URL=postgresql+asyncpg://ivgs:CHANGE_ME_DB_PASSWORD@10.10.0.1:5432/ivgs
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30

# === Redis 7 ===
REDIS_URL=redis://10.10.0.1:6379/0
REDIS_MAX_CONNECTIONS=50

# === SeaweedFS ===
SEAWEEDFS_MASTER_URL=http://10.10.0.1:9333
SEAWEEDFS_FILER_URL=http://10.10.0.1:8888
SEAWEEDFS_MOUNT_PATH=/ivgs

# === Service Ports (§3.1) ===
# FastAPI API: 8001, Next.js Frontend: 3001, GPU Scheduler: 8002
UVICORN_PORT=8001
FRONTEND_PORT=3001
FRONTEND_HOST=localhost

# === GPU Scheduler ===
GPU_SCHEDULER_URL=http://10.10.0.1:8002

# === Retention (§10.3) ===
RETENTION_JOB_CRON=0 2 * * *

# === vLLM Endpoints ===
VLLM_PRIMARY_URL=http://10.10.0.2:8000/v1
VLLM_SECONDARY_URL=http://10.10.0.3:8000/v1
VLLM_MIDSIZE_URL=http://10.10.0.4:8000/v1
OLLAMA_URL=http://10.10.0.5:11434

# === ComfyUI Endpoints ===
COMFYUI_PRIMARY_URL=http://10.10.0.4:8188
COMFYUI_FALLBACK_URL=http://10.10.0.5:8188

# === TTS / Talking Head ===
COQUI_TTS_URL=http://10.10.0.4:5002
KOKORO_TTS_URL=http://10.10.0.4:5003
LATENTSYNC_URL=http://10.10.0.4:7860
SADTALKER_URL=http://10.10.0.4:7861

# === Composition ===
REMOTION_URL=http://10.10.0.6:3002

# === Authentication (§16.1) ===
JWT_SECRET_KEY=CHANGE_ME_STRONG_RANDOM_SECRET_64_CHARS_MINIMUM
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# === Backup (§14.1) ===
BACKUP_NAS_PATH=/mnt/backup/ivgs
BACKUP_GPG_KEY_ID=CHANGE_ME_GPG_KEY_ID

# === NFS Shared Volume ===
SHARED_VOLUME_PATH=/mnt/ivgs-shared

# === Logging (§13.4) ===
LOG_LEVEL=INFO
LOG_FORMAT=json
NODE_HOSTNAME=node-01

# === PROHIBITED — these MUST NOT appear in .env (v5 mandate §7.2) ===
# OPENAI_API_KEY — NEVER
# ANTHROPIC_API_KEY — NEVER
# ELEVENLABS_API_KEY — NEVER
# DID_API_KEY — NEVER
